{% extends 'base.html.twig' %}

{% block title %}Contact - Boutique Femme{% endblock %}

{% block body %}
<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <h2 class="text-center mb-0">Contactez-nous</h2>
                </div>
                <div class="card-body p-4">
                    <p class="text-center text-muted mb-4">
                        Une question ? Une demande particulière ? N'hésitez pas à nous contacter, 
                        nous vous répondrons dans les plus brefs délais.
                    </p>

                    {{ form_start(form) }}
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            {{ form_row(form.name) }}
                        </div>
                        <div class="col-md-6 mb-3">
                            {{ form_row(form.email) }}
                        </div>
                    </div>
                    
                    <div class="mb-3">
                        {{ form_row(form.phone) }}
                    </div>
                    
                    <div class="mb-4">
                        {{ form_row(form.message) }}
                    </div>
                    
                    <div class="mb-3">
                        {{ form_row(form.submit) }}
                    </div>
                    {{ form_end(form) }}

                    <div class="mt-4 pt-4 border-top">
                        <div class="row text-center">
                            <div class="col-md-4 mb-3">
                                <div class="text-primary">
                                    <i class="fas fa-phone fa-2x mb-2"></i>
                                    <h5>Téléphone</h5>
                                    <p class="mb-0">01 23 45 67 89</p>
                                </div>
                            </div>
                            <div class="col-md-4 mb-3">
                                <div class="text-primary">
                                    <i class="fas fa-envelope fa-2x mb-2"></i>
                                    <h5>Email</h5>
                                    <p class="mb-0">contact@boutique-femme.com</p>
                                </div>
                            </div>
                            <div class="col-md-4 mb-3">
                                <div class="text-primary">
                                    <i class="fas fa-clock fa-2x mb-2"></i>
                                    <h5>Horaires</h5>
                                    <p class="mb-0">Lun-Sam: 9h-19h</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}