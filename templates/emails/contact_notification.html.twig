
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: #f8f9fa; padding: 20px; text-align: center; border-radius: 5px; }
        .message-box { background: #f8f9fa; padding: 20px; margin: 20px 0; border-radius: 5px; }
        .contact-info { background: #e9ecef; padding: 15px; border-radius: 5px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Nouveau message de contact</h1>
        </div>
        
        <div class="contact-info">
            <h3>Informations du contact</h3>
            <p><strong>Nom:</strong> {{ contact.name }}</p>
            <p><strong>Email:</strong> {{ contact.email }}</p>
            {% if contact.phone %}
                <p><strong>Téléphone:</strong> {{ contact.phone }}</p>
            {% endif %}
            <p><strong>Date:</strong> {{ contact.createdAt|date('d/m/Y à H:i') }}</p>
        </div>

        <div class="message-box">
            <h3>Message</h3>
            <p>{{ contact.message|nl2br }}</p>
        </div>

        <p>
            <a href="{{ absolute_url(path('admin_contacts')) }}">Voir tous les messages dans l'interface admin</a>
        </p>
    </div>
</body>
</html>